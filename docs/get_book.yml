summary: "Gets book in the system with id"
parameters:
  - name: id
    in: path
    type: string
    description: "book id"
    required: true
responses:
  200:
    description: Returns success message
  400:
    description: Returns failure message